{
  "name": "@ac6_assemble_tool/parts",
  "version": "1.8.1",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "imports": {
    "#parts/*": "./src/*.ts"
  },
  "scripts": {
    "tsc": "tsc",
    "tsc:watch": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint . --cache && prettier . --check --cache",
    "format": "eslint . --cache --fix && prettier . --write --cache",
    "check-types": "tsc --noEmit",
    "generate-part-id": "tsx scripts/part-id/generate-part-id-cli.ts",
    "validate-ids": "tsx scripts/part-id/validate-part-ids.ts",
    "generate-attrs": "tsx scripts/attributes/generate-attributes-cli.ts",
    "build": "pnpm validate-ids"
  },
  "author": "philomagi <tooppoo56@gmail.com> (https://github.com/tooppoo)",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@ac6_assemble_tool/eslint": "workspace:1.0.0",
    "@ac6_assemble_tool/tsconfig": "workspace:1.0.0",
    "ceiocs": "0.0.9"
  },
  "dependencies": {
    "@ac6_assemble_tool/shared": "workspace:1.0.0"
  }
}
